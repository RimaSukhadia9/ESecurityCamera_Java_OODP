
@SuppressWarnings("serial")
public class IPCamera extends SecurityCamera{
	protected String dataRate = "Constant bit rate";

	public IPCamera(){
		
		System.out.println("This is IPCamera with" + dataRate + "datarate, resolution of " + resolution + " with zoom of " + zoom );
		
	}
	
	
	
}
